# Production web build-time env (set these in the Vercel project UI too).
# Copy to .env.production for a local production build (`vite build --mode production`).
VITE_API_BASE_URL=https://sitelens-api.fly.dev
VITE_DEMO_API_KEY=demo-planner-key
